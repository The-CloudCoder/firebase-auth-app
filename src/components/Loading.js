import React from "react";
// import "./Loading.css"; // Import CSS if not in index.css

const Loading = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
